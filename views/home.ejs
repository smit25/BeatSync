<html>
    <head>
        <Title> Homepage</Title>
    </head>
    <body>
        <h2> Welcome to BeatSync</h2>
        <form method ="POST" action = "/home">
            <button type ="submit" name = "createRoom" formaction = "/createRoom"> Create Room </button>
            <button type ="submit" name = "joinRoom" formaction = "/joinRoom"> Join Room </button>
        </form>
        <script>
                function getHashParams() {
                var hashParams = {};
                var e, r = /([^&;=]+)=?([^&;]*)/g,
                q = window.location.hash.substring(1);
                while ( e = r.exec(q)) {
                hashParams[e[1]] = decodeURIComponent(e[2]);
          }
          return hashParams;
        }
               
            var params = getHashParams();

            var access_token = params.access_token,
            refresh_token = params.refresh_token,
            error = params.error;
            console.log(access_token)
            if(access_token != null) {
            var now = new Date();
            now.setTime(now.getTime() + 1 * 3600 * 1000);
            document.cookie = `spotify_token = ${access_token}; expires=${now.toUTCString()}; path = /`
            console.log('spotify_cookie stored')
            } else {
                document.cookie = 'spotify_token=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;'
            }
            document.cookie = 'adminId=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;'
            document.cookie = 'roomUrl=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;'
        </script>
    </body>
</html>
